spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vasujain275/hotels-microservice-configs
          clone-on-start: true

server:
  port: 8084
